#include "main.h"

/**
* postitive_or_negative - check for positive or negative number
*
* @i: the number to be checked
*
* Return: 0
*/

void postitive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i)
	else
		printf("%d is positive\n", i)
			return (0);
}
